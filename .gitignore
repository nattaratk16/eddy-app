# dependencies
/node_modules

# next.js
/.next/
/out/
# โฟลเดอร์ build ชั่วคราวจาก BUILD_DIST_DIR (ดู next.config.js) - ใช้ตอนตรวจว่าโค้ดคอมไพล์ผ่านไหม
/.next-*/

# production
/build

# env files
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# local tooling / logs
.claude/
*.log

# บันทึกการทำงาน/เอกสารสรุปของผู้พัฒนา - เก็บไว้ในเครื่องอย่างเดียว ไม่ต้องเข้า repo
/Work Summary/

# ไฟล์ต้นฉบับงานออกแบบก้อนใหญ่
# (ของที่แอปใช้จริงถูก export/ตัดออกมาไว้ใน public/icons, public/mascot และ app/icon.png แล้ว)
/mascottmodel/ICON PNG/
/mascottmodel/ICON.png
/mascottmodel/eddy_LOGO.png
/mascottmodel/*.mp4
/แบบที่สนใจ.jpg

# คลิปต้นฉบับ/ภาพหน้าจอที่วางไว้ที่ root ของโปรเจกต์ - แอปไม่ได้เรียกใช้
# (ของที่แอปใช้จริงอยู่ใน public/ เช่น public/mascot/eddy-duo-wave.webm ที่แปลงมาจากคลิปต้นฉบับ)
/*.mp4
/แก้1.png
